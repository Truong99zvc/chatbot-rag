# HuggingFace API Token (required for LLM & Embeddings)
# Get your free token at: https://huggingface.co/settings/tokens
HF_TOKEN=your_huggingface_token_here

# LLM model via HF Inference API (optional — default shown)
LLM_MODEL=Qwen/Qwen2.5-7B-Instruct
LLM_TEMPERATURE=0.2

# Embedding model — via HF Inference API (no local GPU/CPU crash issues)
EMBEDDING_MODEL=intfloat/multilingual-e5-large

# FAISS index location (optional — default shown)
FAISS_INDEX_DIR=vectorstores/faiss/current_index

# DB & Cache Configurations (optional - SQLite fallback used by default)
DATABASE_URL=sqlite:///vectorstores/sessions.db
REDIS_URL=

# Langfuse Observability Tracing (optional)
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# Debug mode (optional)
DEBUG=false
